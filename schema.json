{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "unguard configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "paths": {
      "description": "Paths to scan. Defaults to current directory.",
      "type": "array",
      "items": { "type": "string" }
    },
    "ignore": {
      "description": "Glob patterns to ignore.",
      "type": "array",
      "items": { "type": "string" }
    },
    "rules": {
      "description": "Rule severity overrides. Keys are selectors (rule id, wildcard, category:<name>, tag:<name>, confidence:<proven|heuristic>).",
      "type": "object",
      "additionalProperties": {
        "enum": ["off", "info", "warning", "error"]
      }
    },
    "overrides": {
      "description": "Path-scoped rule policies. Each entry applies its rules to diagnostics in files matching the globs (gitignore syntax, relative to the working directory). Later entries win; off drops the diagnostic.",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["files", "rules"],
        "properties": {
          "files": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 1
          },
          "rules": {
            "type": "object",
            "additionalProperties": {
              "enum": ["off", "info", "warning", "error"]
            }
          }
        }
      }
    },
    "failOn": {
      "description": "Exit threshold. Process exits non-zero when diagnostics meet this severity.",
      "enum": ["none", "info", "warning", "error"],
      "default": "info"
    },
    "severity": {
      "description": "Filter output to these severity levels.",
      "type": "array",
      "items": {
        "enum": ["info", "warning", "error"]
      },
      "uniqueItems": true
    },
    "concurrency": {
      "description": "Maximum worker threads used to analyze tsconfig groups in parallel. Auto by default; set to 1 to disable.",
      "type": "integer",
      "minimum": 1
    },
    "cache": {
      "description": "Enable on-disk diagnostic cache under node_modules/.cache/unguard. Default: true.",
      "type": "boolean"
    }
  }
}
