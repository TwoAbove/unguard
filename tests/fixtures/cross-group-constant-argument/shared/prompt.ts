export function renderDraft(draft: string | undefined) {
  return draft ?? "empty";
}
renderDraft(undefined);
renderDraft(undefined);
renderDraft(undefined);

export function withOmission(draft?: string) {
  return draft ?? "empty";
}
withOmission(undefined);
withOmission(undefined);
withOmission(undefined);

export function withSpread(draft: string | undefined) {
  return draft ?? "empty";
}
withSpread(undefined);
withSpread(undefined);
withSpread(undefined);

export function withVariable(draft: string | undefined) {
  return draft ?? "empty";
}
withVariable(undefined);
withVariable(undefined);
withVariable(undefined);
