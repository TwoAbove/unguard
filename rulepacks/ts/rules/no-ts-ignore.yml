id: no-ts-ignore
language: TypeScript
severity: warning
message: Type suppression hides shape errors; fix the type instead of silencing the checker
rule:
  kind: comment
  regex: "@ts-(ignore|expect-error)"
